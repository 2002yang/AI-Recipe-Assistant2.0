# 美食推荐与食谱智能助手 - 运行指南

## 📋 目录

1. [环境要求](#环境要求)
2. [快速开始](#快速开始)
3. [详细安装步骤](#详细安装步骤)
4. [启动项目](#启动项目)
5. [功能测试](#功能测试)
6. [常见问题](#常见问题)
7. [接口文档](#接口文档)

---

## 环境要求

### 必需软件

| 软件 | 版本 | 下载地址 |
|------|------|----------|
| Python | 3.8+ | https://www.python.org/downloads/ |
| Node.js | 16+ | https://nodejs.org/ |
| Git | 任意 | https://git-scm.com/ |

### 验证安装

```bash
# 检查 Python
python --version
# 输出: Python 3.11.x

# 检查 Node.js
node --version
# 输出: v20.x.x

# 检查 npm
npm --version
# 输出: 10.x.x
```

---

## 快速开始

### 方式一：使用启动脚本（推荐）

#### Windows 用户
```bash
# 1. 进入项目目录
cd ai-recipe-assistant

# 2. 双击运行或命令行执行
start.bat
```

#### macOS/Linux 用户
```bash
# 1. 进入项目目录
cd ai-recipe-assistant

# 2. 赋予执行权限
chmod +x start.sh

# 3. 运行
./start.sh
```

**脚本会自动完成：**
- ✅ 检查环境
- ✅ 安装依赖
- ✅ 启动后端（端口 8080）
- ✅ 启动前端（端口 5173）
- ✅ 打开浏览器

---

### 方式二：手动启动

#### 步骤 1：克隆项目
```bash
git clone <项目仓库地址>
cd ai-recipe-assistant
```

#### 步骤 2：启动后端
```bash
cd backend

# 创建虚拟环境
python -m venv venv

# 激活虚拟环境
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt

# 启动服务
python -m uvicorn app.main:app --reload --port 8080
```

**预期输出：**
```
INFO:     Started server process [12345]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://127.0.0.1:8080
```

#### 步骤 3：启动前端
```bash
# 新开一个终端
cd frontend

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

**预期输出：**
```
  VITE v5.0.8  ready in 300 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

#### 步骤 4：访问应用
打开浏览器访问：**http://localhost:5173**

---

## 详细安装步骤

### Windows 详细安装

#### 1. 安装 Python
```powershell
# 下载并安装 Python 3.11
# 安装时勾选 "Add Python to PATH"

# 验证
python --version
```

#### 2. 安装 Node.js
```powershell
# 下载并安装 Node.js LTS 版本
# 下载地址: https://nodejs.org/

# 验证
node --version
npm --version
```

#### 3. 项目设置
```powershell
# 进入项目目录
cd E:\ProjectCollection2026\open-code\ai-hks1

# 后端设置
cd backend
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt

# 前端设置
cd ..\frontend
npm install
```

### macOS 详细安装

```bash
# 使用 Homebrew 安装
brew install python@3.11
brew install node

# 项目设置
cd ai-recipe-assistant

# 后端
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 前端
cd ../frontend
npm install
```

### Linux 详细安装

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install python3.11 python3.11-venv
sudo apt install nodejs npm

# 项目设置（同 macOS）
```

---

## 启动项目

### 开发模式启动

#### 后端（Terminal 1）
```bash
cd backend
venv\Scripts\activate  # Windows
# source venv/bin/activate  # macOS/Linux

# 基础启动
python -m uvicorn app.main:app --reload --port 8080

# 或指定更多参数
python -m uvicorn app.main:app \
    --host 0.0.0.0 \
    --port 8080 \
    --reload \
    --log-level info
```

**启动参数说明：**
- `--reload`: 代码修改自动重启
- `--host 0.0.0.0`: 允许外部访问
- `--port 8080`: 指定端口
- `--log-level`: 日志级别

#### 前端（Terminal 2）
```bash
cd frontend

# 开发模式
npm run dev

# 指定端口
npm run dev -- --port 3000
```

### 生产模式启动

#### 后端生产部署
```bash
cd backend

# 使用 gunicorn (Linux)
pip install gunicorn
gunicorn app.main:app -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8080

# 或使用 uvicorn (Windows)
python -m uvicorn app.main:app --host 0.0.0.0 --port 8080 --workers 4
```

#### 前端生产构建
```bash
cd frontend

# 构建
npm run build

# 预览构建结果
npm run preview
```

---

## 功能测试

### 测试后端 API

#### 1. 健康检查
```bash
curl http://localhost:8080/health

# 预期输出
{"status":"healthy"}
```

#### 2. 根路径测试
```bash
curl http://localhost:8080/

# 预期输出
{"message":"美食推荐与食谱智能助手 API","version":"1.0.0","docs":"/docs"}
```

#### 3. 对话 API 测试
```bash
# 创建新对话
curl -X POST http://localhost:8080/api/chat/new

# 发送消息
curl -X POST http://localhost:8080/api/chat/message \
  -H "Content-Type: application/json" \
  -d '{"message":"我有番茄和鸡蛋，能做什么菜？"}'
```

#### 4. 菜谱列表测试
```bash
# 获取所有菜谱
curl http://localhost:8080/api/recipes/list

# 获取单个菜谱
curl http://localhost:8080/api/recipes/1
```

### 浏览器测试

打开 **http://localhost:5173**，测试以下场景：

#### 测试 1：基础推荐
```
输入：我有番茄、鸡蛋，能做什么？
预期：推荐番茄炒蛋、西红柿鸡蛋汤等
```

#### 测试 2：饮食限制
```
输入：我是素食主义者
预期：推荐素菜，过滤肉类
```

#### 测试 3：营养查询
```
输入：麻婆豆腐的热量是多少？
预期：显示营养成分信息
```

#### 测试 4：食材替代
```
输入：想做宫保鸡丁但没有花生，用什么替代？
预期：提供替代建议（腰果、杏仁等）
```

#### 测试 5：多轮对话
```
第1轮：我想吃鸡蛋
第2轮：还有番茄
预期：记住之前的食材，推荐番茄炒蛋
```

---

## 常见问题

### Q1: 后端启动失败

**问题：** `ModuleNotFoundError: No module named 'fastapi'`

**解决：**
```bash
cd backend
venv\Scripts\activate
pip install -r requirements.txt
```

### Q2: 端口被占用

**问题：** `error while attempting to bind on address ('127.0.0.1', 8080)`

**解决：**
```bash
# 查找占用端口的进程
# Windows
netstat -ano | findstr :8080
taskkill /PID <PID> /F

# macOS/Linux
lsof -ti:8080 | xargs kill -9

# 或使用其他端口
python -m uvicorn app.main:app --port 8081
```

### Q3: 前端无法连接后端

**问题：** 浏览器提示 CORS 错误或无法连接

**解决：**
1. 检查后端是否启动
2. 检查 `frontend/vite.config.ts` 中的代理配置
3. 确保前后端端口正确

### Q4: DeepSeek API 调用失败

**问题：** 意图解析返回错误

**解决：**
- 检查网络连接
- 确认 `DEEPSEEK_API_KEY` 已设置（已内置在代码中）
- 查看后端日志

### Q5: 前端依赖安装失败

**问题：** `npm install` 报错

**解决：**
```bash
# 清除缓存
npm cache clean --force

# 使用淘宝镜像
npm config set registry https://registry.npmmirror.com

# 重新安装
npm install
```

---

## 接口文档

### 后端 API 文档

启动后访问：**http://localhost:8080/docs**

自动生成 Swagger UI 文档，包含：
- 所有 API 端点
- 请求/响应格式
- 在线测试功能

### 主要接口

#### 1. 对话接口
```http
POST /api/chat/message
Content-Type: application/json

{
  "message": "用户输入文本",
  "conversation_id": "对话ID（可选）"
}
```

#### 2. 菜谱接口
```http
# 获取菜谱列表
GET /api/recipes/list

# 获取单个菜谱
GET /api/recipes/{recipe_id}

# 基于食材搜索
POST /api/recipes/search
{
  "ingredients": ["番茄", "鸡蛋"],
  "restrictions": ["素食"]
}
```

#### 3. 营养接口
```http
# 获取菜谱营养信息
GET /api/nutrition/recipe/{recipe_id}

# 检查饮食适合性
GET /api/nutrition/recipe/{recipe_id}/diet/{diet_type}
```

---

## 项目结构

```
ai-recipe-assistant/
├── backend/                    # FastAPI 后端
│   ├── app/
│   │   ├── main.py            # 入口文件
│   │   ├── routers/           # API 路由
│   │   ├── services/          # 业务逻辑
│   │   ├── models/            # 数据模型
│   │   └── data/              # 数据文件
│   ├── venv/                  # Python 虚拟环境
│   ├── requirements.txt       # Python 依赖
│   └── server.log             # 运行日志
│
├── frontend/                   # React 前端
│   ├── src/
│   │   ├── components/        # React 组件
│   │   ├── services/          # API 服务
│   │   └── App.tsx            # 主应用
│   ├── node_modules/          # npm 依赖
│   ├── package.json           # 项目配置
│   └── frontend.log           # 运行日志
│
├── docs/                       # 文档
│   ├── 完整技术流程文档.md
│   ├── LangChain_RAG_完整流程文档.md
│   └── ...
│
├── start.bat                   # Windows 启动脚本
├── start.sh                    # macOS/Linux 启动脚本
└── README.md                   # 项目说明
```

---

## 技术支持

### 查看日志

```bash
# 后端日志
cd backend
tail -f server.log

# 前端日志
cd frontend
tail -f frontend.log
```

### 调试模式

```bash
# 后端调试
cd backend
python -m uvicorn app.main:app --reload --log-level debug

# 前端调试
cd frontend
npm run dev -- --debug
```

---

## 更新日志

### v1.0.0 (2024-01-15)
- ✅ 基础功能：菜谱推荐、营养查询、食材替代
- ✅ 50道精选中餐菜谱
- ✅ DeepSeek AI 集成
- ✅ LangChain + RAG 架构支持

---

**祝您使用愉快！** 🍳👨‍🍳

如有问题，请参考文档或查看项目 README。